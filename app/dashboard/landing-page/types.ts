export type GeneralInfo = {
	name: string;
	description: string;
	slug: string;
};

export type LocationInfo = {
	address: string;
	city: string;
	state: string;
	country: string;
	zipCode?: string;
};
